import { useEffect, useMemo, useState } from "react";
import { ArrowRight, BookOpen, Clock, Gauge, Layers3, Printer, RotateCcw, Sparkles, Target } from "lucide-react";
import { trackEvent } from "./analytics";
import { Breadcrumbs, CopyToClipboard, LessonContent, LessonMetadata, LessonNavigation, PageShell, RelatedContent, ResourceCard, StatusBadge, ToolCard } from "./LearningComponents";
import { NotFoundPage } from "./LearningPages";
import { breadcrumbSchema, usePageMetadata } from "./seo";
import { getInformationalLesson, informationalBase, informationalCases, informationalLastReviewed, informationalLessons, informationalPath, informationalResources, informationalToolLinks } from "./informationalInterviewContent";
import { informationalToolConfigs } from "./informationalInterviewTools";

const graphNode = ({ "@context": _, ...node }) => node;
const tools = Object.values(informationalToolLinks);

function ScopeNotice() {
  return <aside className="lesson-callout"><h2>Una conversación para aprender</h2><p>No es una entrevista de selección, una solicitud encubierta de empleo ni una obligación de referencia. No pidas información confidencial, no grabes ni publiques sin permiso y termina el contacto después de una negativa clara.</p></aside>;
}

export function InformationalHubPage() {
  const breadcrumbs = [{ label: "Inicio", href: "/" }, { label: "Aprende", href: "/aprende" }, { label: "Entrevistas informativas", href: informationalBase }];
  const schema = useMemo(() => ({ "@context": "https://schema.org", "@graph": [graphNode(breadcrumbSchema(breadcrumbs)), { "@type": "LearningResource", name: informationalPath.title, description: informationalPath.description, url: `https://masanes.cl${informationalBase}`, learningResourceType: "Ruta de aprendizaje", educationalLevel: "Inicial a aplicado", dateModified: informationalLastReviewed, inLanguage: "es" }] }), []);
  usePageMetadata({ title: "Entrevistas informativas: aprender antes de decidir", description: informationalPath.description, path: informationalBase, schema });
  useEffect(() => trackEvent("informational_hub_viewed"), []);
  return <PageShell><main id="main-content" className="content-page"><Breadcrumbs items={breadcrumbs}/>
    <header className="page-hero page-hero--networking"><div><p className="eyebrow">Carrera y habilidades profesionales</p><h1>Entrevistas informativas</h1><p className="page-hero__lead">{informationalPath.description}</p><p>Las descripciones públicas rara vez muestran cómo se distribuye el trabajo, qué tensiones importan o cómo cambia una función entre organizaciones. Una conversación deliberada ayuda a contrastar esas incógnitas.</p><div className="hero-actions"><a className="button button--primary" href={informationalLessons[0].href}>Comenzar aquí <ArrowRight size={17}/></a><a className="button button--secondary" href={informationalToolLinks.objective.href}>Clarificar mi objetivo</a><a className="button button--secondary" href={`${informationalBase}/casos`}>Practicar con casos</a></div></div>
      <aside className="summary-panel"><StatusBadge status="published"/><dl><div><dt><Target size={18}/>Dirigido a</dt><dd>{informationalPath.audience}</dd></div><div><dt><BookOpen size={18}/>Formato</dt><dd>{informationalPath.format}</dd></div><div><dt><Clock size={18}/>Dedicación orientativa</dt><dd>{informationalPath.effort}</dd></div><div><dt><Gauge size={18}/>Nivel</dt><dd>Inicial a aplicado</dd></div><div><dt><Layers3 size={18}/>Contenido</dt><dd>10 módulos · 10 herramientas</dd></div></dl></aside>
    </header><ScopeNotice/>
    <section className="guide-intro"><div><p className="eyebrow">Definición y límite</p><h2>Perspectiva para decidir, no acceso para postular</h2><p><strong>{informationalPath.definition}</strong></p><p>Una respuesta, presentación o reunión nunca es una obligación. La persona puede declinar, ofrecer otro formato o terminar la conversación.</p></div><ul>{informationalPath.outcomes.map((item) => <li key={item}>{item}</li>)}</ul></section>
    <section className="content-section content-section--soft"><div className="section-title"><p className="eyebrow">Mapa de conversación con propósito</p><h2>Cinco decisiones, no un guion</h2><p>Aprendizaje buscado, Perspectiva adecuada, Preparación suficiente, Conversación útil y Próximo paso razonado. Es un modelo práctico de esta ruta, no un estándar, certificación ni garantía.</p></div><div className="skill-framework">{["Aprendizaje buscado", "Perspectiva adecuada", "Preparación suficiente", "Conversación útil", "Próximo paso razonado"].map((title, index) => <article key={title}><span>{String(index + 1).padStart(2, "0")}</span><h3>{title}</h3><p>{["Pregunta que necesita evidencia", "Experiencia y límites", "Información pública necesaria", "Escucha y profundización", "Investigación, prueba o decisión"][index]}</p></article>)}</div></section>
    <section className="content-section"><div className="section-title"><p className="eyebrow">Ruta flexible</p><h2>Diez módulos para investigar, conversar y decidir</h2><p>Comienza con el objetivo si aún tienes una curiosidad amplia. Si ya conversaste, puedes ir directamente al registro y comparación.</p></div><div className="case-chapter-grid">{informationalLessons.map((item) => <a className="content-card content-card--link" href={item.href} key={item.slug}><span className="resource-type">Módulo {item.order}</span><h3>{item.title}</h3><p>{item.description}</p><span className="card-link">Abrir módulo <ArrowRight size={16}/></span></a>)}</div></section>
    <section className="content-section content-section--soft"><div className="section-title"><p className="eyebrow">Práctica</p><h2>Casos y recursos para ensayar decisiones</h2><p>Los ejemplos son ficticios, admiten alternativas y evitan datos identificables.</p></div><div className="card-grid"><a className="content-card content-card--link" href={`${informationalBase}/casos`}><h3>24 casos ficticios</h3><p>Practica solicitudes, conversaciones breves, límites y consejos en conflicto.</p><span className="card-link">Abrir biblioteca <ArrowRight size={16}/></span></a><a className="content-card content-card--link" href={`${informationalBase}/recursos`}><h3>12 recursos editables</h3><p>Usa lienzos, matrices, tarjetas, registros y guías imprimibles.</p><span className="card-link">Abrir recursos <ArrowRight size={16}/></span></a><a className="content-card content-card--link" href={`${informationalBase}/practica-integrada`}><h3>Caso Proyecto Umbral</h3><p>Integra investigación, invitación, conversación, evidencia y experimento.</p><span className="card-link">Resolver caso <ArrowRight size={16}/></span></a></div></section>
    <section className="content-section"><div className="section-title"><p className="eyebrow">Herramientas locales</p><h2>Diez ayudas sin cuentas ni almacenamiento</h2><p>No buscan personas, no conectan plataformas, no llaman a una IA y no envían tus textos a analítica.</p></div><div className="card-grid">{tools.map((tool) => <ToolCard key={tool.href} tool={{ ...tool, status: "published" }}/>)}</div></section>
    <section className="content-section content-section--soft"><div className="section-title"><p className="eyebrow">Información temporal</p><h2>Fuentes primarias revisadas</h2><p>Última revisión editorial: 16 de julio de 2026. Se usa lenguaje estable; verifica las condiciones de cualquier plataforma y la normativa local antes de actuar.</p></div><div className="lesson-links"><a href="https://www.w3.org/TR/WCAG22/" target="_blank" rel="noreferrer">W3C: WCAG 2.2 <ArrowRight size={16}/></a><a href="https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data" target="_blank" rel="noreferrer">Google Search Central: datos estructurados <ArrowRight size={16}/></a><a href="https://schema.org/LearningResource" target="_blank" rel="noreferrer">Schema.org: LearningResource <ArrowRight size={16}/></a><a href="https://www.linkedin.com/help/linkedin/answer/a1341387/prohibited-software-and-extensions" target="_blank" rel="noreferrer">LinkedIn: software y automatización no autorizados <ArrowRight size={16}/></a></div></section>
    <section className="content-section"><div className="section-title"><p className="eyebrow">Continúa aprendiendo</p><h2>Rutas relacionadas</h2></div><div className="lesson-links"><a href="/aprende/networking">Networking <ArrowRight size={16}/></a><a href="/aprende/comunicacion-profesional">Comunicación profesional <ArrowRight size={16}/></a><a href="/aprende/adaptabilidad-resiliencia">Adaptabilidad y transiciones <ArrowRight size={16}/></a><a href="/aprende/resolucion-de-problemas">Decisiones con evidencia <ArrowRight size={16}/></a><a href="/aprende/consultoria">Consultoría y preparación profesional <ArrowRight size={16}/></a></div></section>
    <aside className="contextual-cta"><div><h2>¿Necesitas ordenar una transición profesional?</h2><p>Cuando necesitas comparar opciones, contrastar evidencia y convertir conversaciones en decisiones, un proceso de coaching puede ayudarte a estructurar el análisis y la acción.</p></div><a className="button button--primary" href="/#services" onClick={() => trackEvent("informational_coaching_clicked", { destination: "services" })}>Conocer Coaching Profesional</a></aside>
  </main></PageShell>;
}

export function InformationalLessonPage({ slug }) {
  const item = getInformationalLesson(slug); if (!item) return <NotFoundPage/>;
  const breadcrumbs = [{ label: "Inicio", href: "/" }, { label: "Aprende", href: "/aprende" }, { label: "Entrevistas informativas", href: informationalBase }, { label: item.title, href: item.href }];
  const schema = useMemo(() => ({ "@context": "https://schema.org", "@graph": [graphNode(breadcrumbSchema(breadcrumbs)), { "@type": "LearningResource", name: item.title, description: item.description, url: `https://masanes.cl${item.href}`, learningResourceType: "Lección", educationalLevel: item.level, dateModified: item.lastReviewed, inLanguage: "es", isPartOf: { "@type": "LearningResource", name: informationalPath.title, url: `https://masanes.cl${informationalBase}` } }] }), [item.slug]);
  usePageMetadata({ title: item.title, description: item.description, path: item.href, schema });
  useEffect(() => trackEvent("informational_lesson_opened", { module: item.slug }), [item.slug]);
  const resources = informationalResources.slice((item.order - 1) % 8, ((item.order - 1) % 8) + 2);
  const relatedTools = item.relatedTools.map((key) => informationalToolLinks[key]).filter(Boolean);
  return <PageShell><main id="main-content" className="content-page lesson-page"><Breadcrumbs items={breadcrumbs}/><article><header className="lesson-header"><div><p className="eyebrow">Módulo {item.order} de 10</p><h1>{item.title}</h1><p>{item.description}</p></div><StatusBadge status="published"/></header><LessonMetadata module={item} total={10}/><ScopeNotice/><aside className="lesson-outcomes"><h2>Al terminar podrás</h2><ul>{item.outcomes.map((outcome) => <li key={outcome}>{outcome}</li>)}</ul></aside><LessonContent content={item}/><RelatedContent resources={resources} tools={relatedTools} basePath={informationalBase}/><LessonNavigation lessons={informationalLessons} currentSlug={slug}/></article></main></PageShell>;
}

export function InformationalCasesPage() {
  const path = `${informationalBase}/casos`; const breadcrumbs = [{ label: "Inicio", href: "/" }, { label: "Aprende", href: "/aprende" }, { label: "Entrevistas informativas", href: informationalBase }, { label: "Casos", href: path }];
  const schema = useMemo(() => ({ "@context": "https://schema.org", "@graph": [graphNode(breadcrumbSchema(breadcrumbs)), { "@type": "LearningResource", name: "Casos de entrevistas informativas", description: "Biblioteca de casos ficticios para practicar conversaciones profesionales.", url: `https://masanes.cl${path}`, learningResourceType: "Biblioteca de práctica", dateModified: informationalLastReviewed, inLanguage: "es" }] }), []);
  usePageMetadata({ title: "Casos para practicar entrevistas informativas", description: "Practica decisiones, límites y conversaciones con 24 casos ficticios.", path, schema });
  return <PageShell><main id="main-content" className="content-page"><Breadcrumbs items={breadcrumbs}/><header className="page-hero page-hero--compact"><p className="eyebrow">Biblioteca de práctica</p><h1>Casos de entrevistas informativas</h1><p className="page-hero__lead">Situaciones originales con información incompleta, riesgos y más de una acción defendible.</p></header><section className="content-section"><div className="scenario-grid">{informationalCases.map((item) => <article key={item.id}><p className="resource-type">{item.label} · {item.difficulty}</p><h2>{item.title}</h2><p>{item.context}</p><details onToggle={(event) => event.currentTarget.open && trackEvent("informational_case_opened", { category: item.difficulty })}><summary>Analizar el caso</summary><p><strong>Objetivo:</strong> {item.learningObjective}</p><p><strong>Decisión:</strong> {item.decisionPoint}</p><ul>{item.actions.map((action) => <li key={action}>{action}</li>)}</ul><p><strong>Riesgos:</strong> {item.risks.join(", ")}.</p><p><strong>Razonamiento:</strong> {item.feedback}</p><a href={`${informationalBase}/${item.relatedLesson}`}>Abrir lección relacionada <ArrowRight size={15}/></a></details></article>)}</div></section></main></PageShell>;
}

export function InformationalResourcesPage() {
  const path = `${informationalBase}/recursos`; const breadcrumbs = [{ label: "Inicio", href: "/" }, { label: "Aprende", href: "/aprende" }, { label: "Entrevistas informativas", href: informationalBase }, { label: "Recursos", href: path }];
  const schema = useMemo(() => ({ "@context": "https://schema.org", "@graph": [graphNode(breadcrumbSchema(breadcrumbs)), { "@type": "LearningResource", name: "Recursos para entrevistas informativas", description: "Lienzos, matrices, tarjetas y guías editables e imprimibles.", url: `https://masanes.cl${path}`, learningResourceType: "Biblioteca de recursos", dateModified: informationalLastReviewed, inLanguage: "es" }] }), []);
  usePageMetadata({ title: "Recursos para entrevistas informativas", description: "Doce recursos editables e imprimibles para preparar y revisar conversaciones.", path, schema });
  return <PageShell><main id="main-content" className="content-page"><Breadcrumbs items={breadcrumbs}/><header className="page-hero page-hero--compact"><p className="eyebrow">Biblioteca editable</p><h1>Recursos para conversar y decidir</h1><p className="page-hero__lead">Usa descripciones generales o ficticias. Nada se guarda ni se envía.</p></header><section className="content-section"><div className="resource-grid">{informationalResources.map((resource) => <ResourceCard key={resource.id} resource={resource}/>)}</div></section></main></PageShell>;
}

export function InformationalToolsPage({ kind }) {
  const config = informationalToolConfigs[kind] || informationalToolConfigs.objective;
  const initial = useMemo(() => Object.fromEntries(config.fields.map(([name]) => [name, ""])), [kind]);
  const [values, setValues] = useState(initial); const [generated, setGenerated] = useState({ errors: {}, output: "", warnings: [] }); const [resetStatus, setResetStatus] = useState("");
  const breadcrumbs = [{ label: "Inicio", href: "/" }, { label: "Herramientas", href: "/herramientas" }, { label: config.title, href: config.path }];
  const schema = useMemo(() => ({ "@context": "https://schema.org", "@graph": [graphNode(breadcrumbSchema(breadcrumbs)), { "@type": "WebApplication", name: config.title, description: `Herramienta local para ${config.title.toLowerCase()}.`, applicationCategory: "EducationalApplication", operatingSystem: "Web", url: `https://masanes.cl${config.path}`, dateModified: informationalLastReviewed, inLanguage: "es" }] }), [kind]);
  usePageMetadata({ title: config.title, description: `Herramienta privada y local para ${config.title.toLowerCase()}.`, path: config.path, schema });
  useEffect(() => trackEvent("informational_tool_opened", { tool: kind }), [kind]);
  const submit = (event) => { event.preventDefault(); const next = config.build(values); setGenerated(next); setResetStatus(""); if (!Object.keys(next.errors || {}).length) trackEvent(config.event, { tool: kind }); };
  const reset = () => { setValues(initial); setGenerated({ errors: {}, output: "", warnings: [] }); setResetStatus("Formulario y resultado reiniciados"); };
  return <PageShell><main id="main-content" className="content-page"><Breadcrumbs items={breadcrumbs}/><header className="page-hero page-hero--compact"><p className="eyebrow">Herramienta local y determinista</p><h1>{config.title}</h1><p className="page-hero__lead">Organiza tu preparación sin cuentas, almacenamiento, búsqueda de personas ni servicios externos.</p><p>No ingreses nombres, empleadores, correos, perfiles, mensajes privados, notas identificables ni información confidencial. Los datos permanecen en esta sesión.</p></header><section className="generator-layout"><form className="generator-form" onSubmit={submit} noValidate>{config.fields.map(([name, label, type = "textarea", options]) => <label className="field" htmlFor={`informational-${kind}-${name}`} key={name}><span>{label}</span>{type === "select" ? <select id={`informational-${kind}-${name}`} value={values[name]} aria-invalid={Boolean(generated.errors?.[name])} aria-describedby={generated.errors?.[name] ? `informational-error-${kind}-${name}` : undefined} onChange={(event) => setValues((current) => ({ ...current, [name]: event.target.value }))}><option value="">Selecciona una opción</option>{options.map((option) => <option key={option}>{option}</option>)}</select> : <textarea id={`informational-${kind}-${name}`} rows="3" maxLength="2000" value={values[name]} aria-invalid={Boolean(generated.errors?.[name])} aria-describedby={generated.errors?.[name] ? `informational-error-${kind}-${name}` : undefined} onChange={(event) => setValues((current) => ({ ...current, [name]: event.target.value }))}/>} {generated.errors?.[name] && <small className="field-error" id={`informational-error-${kind}-${name}`}>{generated.errors[name]}</small>}</label>)}<div className="form-actions"><button className="button button--primary" type="submit"><Sparkles size={16}/>Crear ayuda</button><button className="button button--secondary" type="button" onClick={reset}><RotateCcw size={16}/>Limpiar</button><span className="sr-only" role="status" aria-live="polite">{resetStatus}</span></div></form><aside className="generator-output" aria-live="polite"><h2>Resultado</h2>{Object.keys(generated.errors || {}).length > 0 && <div role="alert"><p>Revisa los campos señalados.</p></div>}{generated.output ? <><pre className="template-preview">{generated.output}</pre>{generated.warnings?.length > 0 && <div className="tool-warnings"><h3>Límites</h3><ul>{generated.warnings.map((warning) => <li key={warning}>{warning}</li>)}</ul></div>}<div className="form-actions"><CopyToClipboard text={generated.output} label="Copiar resultado" onCopy={() => trackEvent("informational_resource_copied", { tool: kind })}/><button className="button button--secondary" type="button" onClick={() => { trackEvent("informational_resource_printed", { tool: kind }); window.print(); }}><Printer size={16}/>Imprimir</button></div></> : <p>Completa los campos obligatorios. La salida organiza información declarada; no evalúa tu empleabilidad, no garantiza respuestas y no determina normas sociales con certeza.</p>}</aside></section></main></PageShell>;
}
